package ${package}.controller.${moduleName};

import com.sw.base.controller.BaseController;
import ${package}.service.impl.${moduleName}.${className}ServiceImpl;
import ${packageEntity}.entity.${moduleName}.${className};
import org.springframework.stereotype.Controller;
import org.springframework.web.bind.annotation.RequestMapping;
import org.slf4j.Logger;
import org.slf4j.LoggerFactory;

@Controller
@RequestMapping("/system-web/${secondModuleName}")
public class ${className}Controller extends BaseController<${className}ServiceImpl,${className}> {

    private static final Logger LOGGER = LoggerFactory.getLogger(${className}Controller.class);

}