package ${package}.mapper.${moduleName};

import ${packageEntity}.entity.${moduleName}.${className};
import com.baomidou.mybatisplus.mapper.BaseMapper;

/**
 * ${comments}
 * 
 * @author ${author}
 * @email ${email}
 * @date ${datetime}
 */
public interface ${className}Mapper extends BaseMapper<${className}> {
	
}
